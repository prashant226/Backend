function counter(start, end, delay) {
    console.log(start);

    // if (start < end) {
    //     setTimeout(function() {
    //         counter(start + 1, end, delay);
    //     }, delay);
    // }
}

// Example usage: Count from 1 to 5 with a delay of 1000 milliseconds (1 second)
counter(1, 5, 1000);